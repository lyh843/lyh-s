#include<stdio.h>
#include<stdbool.h>
#include<string.h>
#define N 35
bool judge = true;
char book[17] = "0123456789ABCDEF";
// void f_judge(char ch_number[N], int decimal)
// {
//     for(int i = 0; ch_number[i] != '\0'; i++)
//     {
//         if(decimal > (strchr(book, ch_number[i]) - ))
//     }
// }
int main(void)
{
    int len = 0, decimal = 0;
    char ch_number[N] = {0};
    scanf("%d%d", &len, &decimal);
    scanf("%s", ch_number);
}